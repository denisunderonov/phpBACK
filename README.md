# PHP Backend - Лабораторные работы

## Короткое описание

Учебный репозиторий с лабораторными работами по курсу "Серверное программирование" (PHP + MySQL). Содержит базовые примеры работы с PHP, обработку форм, взаимодействие с базой данных и полнофункциональное CRUD-приложение для управления заметками.

## Содержание

### 📝 homework1 - Домашнее задание
- `index.php` — базовый пример PHP-скрипта
- Вывод текста, основы синтаксиса

### 🔰 lab1 - Основы PHP
- `index.php` — первая лабораторная работа
- "Hello World" на PHP
- Базовая структура HTML + PHP

### 📋 lab2 - Работа с формами
- `form.html` — HTML-форма для ввода данных
- `back.php` — обработка данных формы на сервере
- Передача данных методом POST/GET

### 🧮 lab3 - Калькулятор (AJAX + PHP)
- `index.html` — интерфейс калькулятора
- `server.php` — серверная логика вычислений
- `script.js` — AJAX-запросы к серверу
- `style.css` — стили интерфейса
- **Функционал:**
  - Вычисление математических выражений
  - Поддержка скобок и приоритета операций
  - Асинхронная обработка через AJAX

### 📒 notelist - CRUD-приложение для заметок

**Полнофункциональное приложение** для управления записями в базе данных MySQL.

#### Файлы:

**Главные страницы:**
- `index.php` — главная страница приложения
- `menu.php` — верхнее меню навигации
- `viewer.php` — просмотр всех записей

**Добавление:**
- `add.php` — страница добавления записи
- `form.php` — форма для ввода новой записи

**Редактирование:**
- `edit.php` — выбор записи для редактирования
- `edit_record.php` — обработка редактирования
- `form_edit.php` — форма редактирования

**Удаление:**
- `delete.php` — выбор записи для удаления
- `delete_record.php` — обработка удаления

**Вспомогательные:**
- `data.php` — подключение к базе данных (MySQL)
- `style.css` — стили приложения

#### Функционал notelist:
- ✅ **Create** — добавление новых записей
- ✅ **Read** — просмотр всех записей в таблице
- ✅ **Update** — редактирование существующих записей
- ✅ **Delete** — удаление записей
- ✅ Навигационное меню
- ✅ Работа с MySQL через mysqli

## Требования

### Серверное окружение:
- **PHP** 7.4+ (рекомендуется 8.0+)
- **MySQL** 5.7+ или MariaDB 10.2+
- **Apache** или **Nginx** (с поддержкой PHP)

### Локальная разработка:
Один из вариантов:
- **XAMPP** (Windows/Mac/Linux)
- **MAMP** (Mac/Windows)
- **OpenServer** (Windows)
- **Docker** с PHP + MySQL

## Настройка

### 1. База данных (для notelist)

Создайте базу данных и таблицу:

```sql
CREATE DATABASE notebook;
USE notebook;

CREATE TABLE notes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    content TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 2. Конфигурация подключения

Отредактируйте `notelist/data.php`:

```php
$db_host = 'localhost';    // Хост БД
$db_user = 'root';         // Пользователь БД
$db_password = '';         // Пароль (по умолчанию пустой для XAMPP/MAMP)
$db_name = 'notebook';     // Имя базы данных
```

### 3. Запуск

#### Вариант 1: XAMPP/MAMP
1. Скопируйте папку проекта в `htdocs/` (XAMPP) или `htdocs/` (MAMP)
2. Запустите Apache и MySQL через панель управления
3. Откройте в браузере: `http://localhost/phpBACK/notelist/`

#### Вариант 2: Встроенный сервер PHP (для тестирования)
```bash
cd phpBACK
php -S localhost:8000
```
Откройте: `http://localhost:8000/notelist/`

**Важно:** Встроенный сервер PHP не поддерживает `.htaccess` и некоторые функции Apache.

## Структура проекта

```
phpBACK/
├── homework1/
│   └── index.php              # Домашнее задание
├── lab1/
│   └── index.php              # ЛР1: Hello World
├── lab2/
│   ├── form.html              # Форма ввода
│   └── back.php               # Обработка формы
├── lab3/
│   ├── index.html             # Интерфейс калькулятора
│   ├── server.php             # Серверная логика
│   ├── script.js              # AJAX-запросы
│   └── style.css              # Стили
└── notelist/                  # CRUD-приложение
    ├── index.php              # Главная страница
    ├── menu.php               # Навигация
    ├── data.php               # Подключение к БД
    ├── viewer.php             # Просмотр записей
    ├── add.php / form.php     # Добавление
    ├── edit.php / edit_record.php / form_edit.php  # Редактирование
    ├── delete.php / delete_record.php              # Удаление
    └── style.css              # Стили
```

## Использование

### Notelist - Управление заметками

**1. Просмотр записей:**
- Перейдите в раздел "Просмотр"
- Отобразится таблица со всеми записями

**2. Добавление записи:**
- Нажмите "Добавление записи"
- Заполните форму (заголовок, текст)
- Нажмите "Сохранить"

**3. Редактирование:**
- Выберите "Редактирование записи"
- Выберите запись из списка
- Измените данные в форме
- Сохраните изменения

**4. Удаление:**
- Выберите "Удаление записи"
- Выберите запись для удаления
- Подтвердите действие

### Lab3 - Калькулятор

- Откройте `lab3/index.html`
- Введите математическое выражение
- Результат вычислится на сервере через AJAX
- Поддерживаются: `+`, `-`, `*`, `/`, скобки `()`

## Технологии

- **PHP** — серверный язык программирования
- **MySQL** (mysqli) — реляционная база данных
- **HTML5** — структура страниц
- **CSS3** — стилизация
- **JavaScript** (AJAX) — асинхронные запросы (lab3)

## Архитектура notelist

### Паттерн:
Простая процедурная архитектура с разделением на:
- **Представление** (HTML-формы)
- **Логика** (PHP-обработчики)
- **Данные** (MySQL через mysqli)

### Безопасность:
⚠️ **Учебный проект** — не использовать в production без доработки:
- Добавить валидацию данных
- Использовать подготовленные запросы (prepared statements)
- Добавить защиту от SQL-инъекций
- Реализовать аутентификацию
- Использовать HTTPS

## Возможные улучшения

### Для notelist:
- [ ] Добавить пагинацию (постраничный вывод)
- [ ] Реализовать поиск по записям
- [ ] Добавить категории/теги
- [ ] Улучшить валидацию форм (client + server)
- [ ] Использовать PDO вместо mysqli
- [ ] Добавить AJAX для обновления без перезагрузки
- [ ] Реализовать систему пользователей

### Для проекта в целом:
- [ ] Добавить .gitignore
- [ ] Вынести конфиг БД в отдельный файл
- [ ] Использовать переменные окружения (.env)
- [ ] Добавить обработку ошибок
- [ ] Логирование действий

## Автор

Денис Андронов  
Андреевич

## Примечания

Это учебный проект, созданный для изучения основ PHP и работы с базами данных.
